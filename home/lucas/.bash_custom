# Bash prompt.
	
	# Bold colours.
	GREEN_BD="\[\e[1;38;5;034m\]"
	YELLOW_BD="\[\e[1;38;5;220m\]"
	YELLOW2_BD="\[\e[1;38;5;226m\]"
	RED_BD="\[\e[1;38;5;196m\]"
	SKYBLUE_BD="\[\e[1;38;5;027m\]"
	PINK_BD="\[\e[1;38;5;212m\]"

	# Miscellaneous.
	RESET="\[\033[0;0m\]"
	
	# Prompt format.
	PS1="${RED_BD}[${RESET}${YELLOW_BD}\u${RESET}${GREEN_BD}@${RESET}${SKYBLUE_BD}\h${RESET} ${PINK_BD}\w${RESET}${RED_BD}]${RESET} ${YELLOW2_BD}⚡${RESET} "

# Variables.
github="$HOME/Documentos/GitHub"

# Aliases.

	# Directories.
	alias gitdir="cd $github"

	# Scripts.
	alias copy="cd $github/scripts/python && python copyconfigs.py && cd"
	alias adbscript="cd $github/scripts/python && python adbscript.py && cd"
	alias genscript="cd $github/scripts/python && python genscript.py && cd"

	# Miscellaneous.
	alias battery="cat /sys/class/power_supply/BAT*/capacity"
	alias makepdf="magick \*.png my_pdf.pdf"
	alias wikibot="cd Documentos/bot && source bin/activate && python pwb.py login"

# Functions.
scrub_patch() {
	# Taken from: https://devmanual.gentoo.org/ebuild-writing/misc-files/patches/index.html
	sed -i \
		-e '/^index /d' \
		-e '/^new file mode /d' \
		-e '/^Index:/d' \
		-e '/^=========/d' \
		-e '/^RCS file:/d' \
		-e '/^retrieving/d' \
		-e '/^diff/d' \
		-e '/^Files .* differ$/d' \
		-e '/^Only in /d' \
		-e '/^Common subdirectories/d' \
		-e '/^deleted file mode [0-9]*$/d' \
		-e '/^+++/s:\t.*::' \
		-e '/^---/s:\t.*::' \
		"$@"
}
